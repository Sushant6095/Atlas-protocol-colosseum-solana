[package]
name = "atlas-qvac"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Atlas Local-AI Layer — Tether QVAC integration for pre-sign explanations, invoice OCR, alert translation, and second-opinion review. Implements directive 19."

[lib]

[dependencies]
atlas-runtime = { path = "../atlas-runtime" }
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
thiserror.workspace = true
blake3.workspace = true

[dev-dependencies]
proptest = "1.5"
