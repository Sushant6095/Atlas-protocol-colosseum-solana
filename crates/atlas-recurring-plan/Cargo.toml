[package]
name = "atlas-recurring-plan"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Adaptive DCA — Jupiter Recurring + AI-modulated cadence with proof-gated parameter updates. Implements directive 12 §4."

[lib]

[dependencies]
atlas-runtime = { path = "../atlas-runtime" }
atlas-failure = { path = "../atlas-failure" }
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
thiserror.workspace = true
blake3.workspace = true

[dev-dependencies]
proptest = "1.5"
