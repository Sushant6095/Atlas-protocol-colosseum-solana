[package]
name = "atlas-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Atlas Solana runtime constraints — account locks, compute budget, tx size envelope, zero-copy invariants. Implements directive 07 §1-§3, §9-§11."

[lib]

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
thiserror.workspace = true
blake3.workspace = true

[dev-dependencies]
proptest = "1.5"
