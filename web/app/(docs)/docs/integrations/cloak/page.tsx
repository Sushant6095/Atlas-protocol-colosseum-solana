// /docs/integrations/cloak — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Cloak"
      description="Confidential treasury."
      intro="Cloak provides confidential treasury rails on Solana. Atlas integrates Cloak's view-key model into the treasury entity layer."
    />
  );
}
