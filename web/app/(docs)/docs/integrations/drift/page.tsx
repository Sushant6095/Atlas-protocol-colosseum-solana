// /docs/integrations/drift — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Drift"
      description="Drift CPI."
      intro="Drift is a perp DEX and a yield venue (Drift Earn). Atlas integrates the lending pools and uses Drift for cash-and-carry hedges; this page covers both."
    />
  );
}
