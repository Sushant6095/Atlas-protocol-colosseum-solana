// /docs/integrations/magicblock — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="MagicBlock"
      description="Private execution rooms."
      intro="MagicBlock provides private execution rooms (PER) — a side-VM that settles back to mainnet inside a fixed slot budget. Atlas uses PER for MEV-sensitive flow."
    />
  );
}
