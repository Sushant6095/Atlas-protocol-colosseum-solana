// /docs/integrations/marginfi — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="marginfi"
      description="marginfi CPI."
      intro="marginfi is a borrow / lend venue. Atlas uses it for short-end USDC yield and as a borrow leg in delta-neutral strategies."
    />
  );
}
