// /docs/integrations — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Integrations"
      description="Every venue, oracle, and infra partner Atlas connects to."
      intro="Atlas is a routing layer, not a destination. This index lists every venue (Kamino, Drift, Jupiter, marginfi, ...), every oracle (Pyth, Switchboard), every infra partner (Helius, Triton, QuickNode, ...), and the proof and treasury rails that make the platform work."
    />
  );
}
