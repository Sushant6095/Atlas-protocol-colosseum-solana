// /docs/integrations/pusd — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Palm USD"
      description="Reserve asset."
      intro="Palm USD (PUSD) is a reserve-backed stablecoin. Atlas treats it as a parking asset for treasury reserves, with attestation pulled into the unified ledger."
    />
  );
}
