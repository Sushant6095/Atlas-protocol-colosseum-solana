// /docs/integrations/quicknode — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="QuickNode"
      description="Fee oracle and WSS stream."
      intro="QuickNode powers the fee oracle that drives Atlas's compute-unit pricing and the WebSocket stream that backs live UI surfaces."
    />
  );
}
