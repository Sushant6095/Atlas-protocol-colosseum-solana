// /docs/integrations/rpc-fast — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="RPC Fast"
      description="Latency tier-A RPC."
      intro="RPC Fast is the latency tier-A RPC partner for transaction submission. Atlas pins write paths to RPC Fast for the lowest landing-time variance."
    />
  );
}
