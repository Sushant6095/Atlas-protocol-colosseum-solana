// /docs/integrations/succinct-sp1 — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Succinct SP1"
      description="SP1 zkVM and verifier."
      intro="Atlas proofs are generated inside Succinct's SP1 zkVM. We use the SP1 prover network for generation and sp1-solana for on-chain verification."
    />
  );
}
