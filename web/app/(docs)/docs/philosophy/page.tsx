// /docs/philosophy — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Philosophy"
      description="What Atlas believes and why."
      intro="Atlas is opinionated. This section documents the beliefs that shaped the system — why we built it, what we promise, what discipline we hold to, and what we explicitly refused to ship even when we could have."
    />
  );
}
