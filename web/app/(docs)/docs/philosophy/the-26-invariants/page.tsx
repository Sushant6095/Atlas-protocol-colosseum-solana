// /docs/philosophy/the-26-invariants — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="The 26 invariants"
      description="Each promise, in plain language."
      intro="Atlas ships with 26 invariants. Each one is a promise that holds no matter what strategy is loaded, what model is in use, or what venue is being routed to. This page lists every invariant in plain language with the on-chain check that enforces it."
    />
  );
}
