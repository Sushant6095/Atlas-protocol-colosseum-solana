// /docs/philosophy/what-we-refused-to-build — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="What we refused to build"
      description="The cuts list."
      intro="Things we could have shipped and did not — points capture, hidden venue subsidies, a private model marketplace, native token speculation. This page documents the cuts and why."
    />
  );
}
