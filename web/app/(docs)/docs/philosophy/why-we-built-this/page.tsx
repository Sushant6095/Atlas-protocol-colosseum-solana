// /docs/philosophy/why-we-built-this — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Why we built this"
      description="The thesis behind Atlas."
      intro="We started Atlas because every existing on-chain treasury product asked you to trust a team. We thought it was possible to build one that did not — that proved every move with math, in public, and let users verify each step themselves. This page lays out the argument."
    />
  );
}
