// /docs/sdk/rust — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="atlas-rs"
      description="Rust SDK and PDA helpers."
      intro="atlas-rs mirrors the TypeScript surface and adds PDA helpers, an on-chain proof-shape verifier, and the verify-inference CPI primitive any Solana program can call. Suitable for high-throughput services and on-chain integrators."
    />
  );
}
