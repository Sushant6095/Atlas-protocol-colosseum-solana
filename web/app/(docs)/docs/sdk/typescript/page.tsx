// /docs/sdk/typescript — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="@atlas/sdk"
      description="The browser and Node TypeScript client for Atlas."
      intro="@atlas/sdk wraps the REST and WebSocket surface, exposes typed responses, and bundles a pure-JS proof-shape verifier. This page walks installation, authentication, and the most-used calls."
    />
  );
}
