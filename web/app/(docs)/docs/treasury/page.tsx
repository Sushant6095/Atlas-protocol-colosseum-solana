// /docs/treasury — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Atlas Treasury OS"
      description="Treasury operations for DAOs, startups, and businesses."
      intro="Treasury OS is the layer that takes Atlas Vault and turns it into a real treasury system: multisig custody, runway forecasting, scheduled payments, invoice intelligence, audit-ready ledger. Built for finance teams who want yield without giving up control."
    />
  );
}
