// /docs/treasury/runway-forecast — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Runway forecast"
      description="Months of runway under load, with proof."
      intro="Treasury teams care about one number: how many months of runway are left. Atlas runs the calculation against actual payment schedules, current yield, and stress scenarios — and ships every forecast as a verifiable artifact your CFO can hand to a board."
    />
  );
}
