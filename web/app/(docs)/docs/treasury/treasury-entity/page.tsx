// /docs/treasury/treasury-entity — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Treasury entity"
      description="Squads multisig and KYB onboarding."
      intro="An Atlas treasury entity is a Squads multisig with KYB attached. This page walks the onboarding wizard, the role model, signer rotation, and how the entity hooks into payment, runway, and ledger views."
    />
  );
}
