// /docs/treasury/unified-ledger — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Unified ledger"
      description="One audit-ready timeline across every layer."
      intro="Vault movements, payment instructions, invoice approvals, governance votes, and proof receipts in a single chronological view. Export-friendly, signed, and reproducible from on-chain state."
    />
  );
}
