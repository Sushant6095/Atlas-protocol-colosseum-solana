// /docs/vault — stub. Real content ships in the next PR.

"use client";

import { DocStub } from "@/components/docs";

export default function Page(): JSX.Element {
  return (
    <DocStub
      title="Atlas Vault"
      description="The user product: stablecoins in, verified yield out."
      intro="Atlas Vault is the consumer-facing product. Deposit USDC, the autonomous treasurer routes it across the best venues, and every move ships with a proof you can verify in your browser. This page is the table of contents for everything a depositor needs."
    />
  );
}
